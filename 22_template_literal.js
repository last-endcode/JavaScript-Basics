// template literal ` `
// insert expression ${value}

let name = 'john';
let age = 25;

const result = `Today ${name} , already ${age} years old
and here u can calculate too 4 x 5 is : ${4 * 5}`;
console.log(result);
